<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/round_background"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.carpool.tagalong.views.RegularTextView
        android:id="@+id/cardNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:textColor="@android:color/black"
        app:layout_constraintLeft_toLeftOf="parent"
        android:padding="@dimen/_5_px"
        android:text="@string/recent_rides"/>

    <CheckBox
        android:id="@+id/default_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:backgroundTint="@color/colorPrimary"
        android:drawableTint="@color/colorAccent"
        android:layout_marginRight="@dimen/_20_px"
        app:layout_constraintRight_toLeftOf="@id/deleteCard"/>

    <ImageView
        android:id="@+id/deleteCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@id/default_check"
        app:layout_constraintBottom_toBottomOf="@id/default_check"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="20dp"
        android:src="@drawable/ic_delete_driving_xxhdpi"

        />

</android.support.constraint.ConstraintLayout>